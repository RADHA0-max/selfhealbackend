route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1m
  receiver: 'nanoclaw'

receivers:
  - name: 'nanoclaw'
    webhook_configs:
      - url: 'http://host.docker.internal:8081/alerts'
        send_resolved: true
