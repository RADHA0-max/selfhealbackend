global:
  scrape_interval: 5s
  evaluation_interval: 5s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
          - 'alertmanager:9093'

rule_files:
  - "alert-rules.yml"

scrape_configs:
  - job_name: 'mock-server'
    static_configs:
      - targets: ['mock-server:8080']